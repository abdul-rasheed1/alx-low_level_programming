#include <stdio.h>
#include "main.h"
/**
 * main - entry point
 * @argc: argument count
 * @argv: argument vector containing strings of the args
 *
 * Return: 0 always succes
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
